services:
  app:
    image: node:24
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    working_dir: /workspace
    networks:
      - local

networks:
  local:
    external: true
